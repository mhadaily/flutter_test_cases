name: example_analyzer_plugin
description:
  Custom analyzer plugin with lint rules for Dart and Flutter projects
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  analyzer: ^9.0.0
  analyzer_plugin: ^0.13.11
  analysis_server_plugin: ^0.3.4

dev_dependencies:
  lints: ^6.0.0
